from setuptools import setup, find_packages

setup(
    name="rbdd_project",
    version="1.0.0",
    description="RBDD - Real-Time Block-Level Dual-Mode Data Deduplication",
    packages=find_packages(),
    install_requires=[
        # Dependencies listed in requirements.txt
    ],
)
